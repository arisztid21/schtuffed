import React, { Component } from 'react';

export default class Shop extends Component {
    render() {
        return (
            <div>
                this is the shop and is being tested to render
            </div>
        );
    }
}